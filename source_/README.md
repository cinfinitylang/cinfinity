# Build guide

## First step (compile 'new')
* **g++ -o new newfile.c++**

## Commands ('new')
* **new:**        compile-only
* **new prerun:** compile + run
* **new run:**    run
* **new void:**   clean
